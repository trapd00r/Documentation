# I keep an `xkb' configuration in ~/etc/xkb. This reloads my keyboard setup
# without having to do any manual work.

emulate -L zsh
local xkbdir

xkbdir="${HOME}/etc/xkb"
xkbcomp -I"${xkbdir}" "${xkbdir}/ft.xkb" "${DISPLAY}"
