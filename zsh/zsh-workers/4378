From zsh-workers-request@math.gatech.edu Thu Sep 03 15:08:15 1998
Return-Path: <zsh-workers-request@math.gatech.edu>
Delivered-To: mason-zsh@primenet.com.au
Received: (qmail 9751 invoked from network); 3 Sep 1998 15:08:14 -0000
Received: from math.gatech.edu (list@130.207.146.50)
  by ns1.primenet.com.au with SMTP; 3 Sep 1998 15:08:14 -0000
Received: (from list@localhost)
	by math.gatech.edu (8.9.1/8.9.1) id LAA27746;
	Thu, 3 Sep 1998 11:01:40 -0400 (EDT)
Resent-Date: Thu, 3 Sep 1998 11:01:40 -0400 (EDT)
From: "Bart Schaefer" <schaefer@brasslantern.com>
Message-Id: <980903080427.ZM3968@candle.brasslantern.com>
Date: Thu, 3 Sep 1998 08:04:27 -0700
In-Reply-To: <199809030543.AAA18620@hzoli.home>
Comments: In reply to Zoltan Hidvegi <hzoli@cs.elte.hu>
        "Re: zsh-3.1.4 bug, parsing case ... esac" (Sep  3, 12:43am)
References: <199809030543.AAA18620@hzoli.home>
X-Mailer: Z-Mail (4.0b.820 20aug96)
To: zsh-workers@math.gatech.edu
Subject: PATCH: 3.0.5 - Re: zsh-3.1.4 bug, parsing case ... esac
MIME-Version: 1.0
Content-Type: text/plain; charset=us-ascii
Resent-Message-ID: <"nfzTE2.0.Qn6.Kzgxr"@math>
Resent-From: zsh-workers@math.gatech.edu
X-Mailing-List: <zsh-workers@math.gatech.edu> archive/latest/4378
X-Loop: zsh-workers@math.gatech.edu
Precedence: list
Resent-Sender: zsh-workers-request@math.gatech.edu

On Sep 3, 12:43am, Zoltan Hidvegi wrote:
} Subject: Re: zsh-3.1.4 bug, parsing case ... esac
} > }
} > } zsh: parse error near `echo'
} > } zsh: parse error in command substitution
} > } 
} > } 		esac else echo bison ;
} 
} Actually, the shell grammar allows this, this is really a zsh bug.

Here's Zoltan's patch tweaked for 3.0.5.

Index: Src/parse.c
===================================================================
--- parse.c	1998/07/02 15:56:02	1.6
+++ parse.c	1998/09/03 14:55:26
@@ -482,16 +482,12 @@
 
 	while (tok == SEPER)
 	    yylex();
-	if (tok == OUTBRACE) {
-	    yylex();
+	if (tok == OUTBRACE)
 	    break;
-	}
 	if (tok != STRING)
 	    YYERRORV;
-	if (!strcmp(tokstr, "esac")) {
-	    yylex();
+	if (!strcmp(tokstr, "esac"))
 	    break;
-	}
 	str = tokstr;
 	incasepat = 0;
 	incmdpos = 1;
@@ -556,16 +552,17 @@
 	addlinknode(pats, str);
 	addlinknode(lists, par_list());
 	n++;
-	if ((tok == ESAC && !brflag) || (tok == OUTBRACE && brflag)) {
-	    yylex();
+	if ((tok == ESAC && !brflag) || (tok == OUTBRACE && brflag))
 	    break;
-	}
 	if (tok != DSEMI)
 	    YYERRORV;
 	incasepat = 1;
 	incmdpos = 0;
 	yylex();
     }
+
+    incmdpos = 1;
+    yylex();
 
     cc->pats = (char **)alloc((n + 1) * sizeof(char *));
 


-- 
Bart Schaefer                                 Brass Lantern Enterprises
http://www.well.com/user/barts              http://www.brasslantern.com

