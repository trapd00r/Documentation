### vim:ft=zsh:foldmethod=marker

for mod in parameter zle zleparameter \
           compctl complete complist computil \
           deltochar athfunc curses pcre bar zutil ; do
    zrcmodload zsh/${mod}
done

zrcmodload -ab zsh/stat stat

unset mod
