### vim:ft=zsh:foldmethod=marker
### cvs diff with diff stat using just one network access.
emulate -L zsh

local -a diff

diff=( ${(f)"$(cvs diff)"} )
print -l "${diff[@]}" | diffstat -p0
print
print -l "${diff[@]}"
