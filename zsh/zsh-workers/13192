From zsh-workers-return-13192-mason-zsh=primenet.com.au@sunsite.auc.dk Sat Nov 25 19:07:26 2000
Return-Path: <zsh-workers-return-13192-mason-zsh=primenet.com.au@sunsite.auc.dk>
Delivered-To: mason-zsh@primenet.com.au
Received: (qmail 28259 invoked from network); 25 Nov 2000 19:07:26 -0000
Received: from sunsite.dk (HELO sunsite.auc.dk) (130.225.51.30)
  by ns1.primenet.com.au with SMTP; 25 Nov 2000 19:07:26 -0000
Received: (qmail 16410 invoked by alias); 25 Nov 2000 19:07:18 -0000
Mailing-List: contact zsh-workers-help@sunsite.auc.dk; run by ezmlm
Precedence: bulk
X-No-Archive: yes
Delivered-To: mailing list zsh-workers@sunsite.auc.dk
X-Seq: 13192
Received: (qmail 16400 invoked from network); 25 Nov 2000 19:07:16 -0000
From: "Bart Schaefer" <schaefer@candle.brasslantern.com>
Message-Id: <1001125190703.ZM15525@candle.brasslantern.com>
Date: Sat, 25 Nov 2000 19:07:03 +0000
In-Reply-To: <20001126.010828.90831447.m5032106@u-aizu.ac.jp>
Comments: In reply to  Naoki Wakamatsu <m5032106@u-aizu.ac.jp>
        "coredump on dev-7 (ALL_EXPORT)" (Nov 26,  1:08am)
References: <20001126.010828.90831447.m5032106@u-aizu.ac.jp>
X-Mailer: Z-Mail (5.0.0 30July97)
To: Naoki Wakamatsu <m5032106@u-aizu.ac.jp>, zsh-workers@sunsite.auc.dk
Subject: PATCH: Re: coredump on dev-7 (ALL_EXPORT)
MIME-Version: 1.0
Content-Type: text/plain; charset=us-ascii

The completion system definitely should not use ALL_EXPORT.

The real problem, however, is that arrays and hashes can't be exported.
I've stubbed in some #if 0 code to handle ksh-style array export as was
discussed way back in zsh-workers/9576 and related messages, but there
are a number of other places that check for arrays or hashes and either
skip calling export_param or don't set the PM_EXPORTED flag, and I don't
have time to conduct a thorough search or testing pass for all of them.

Index: Completion/Core/compinit
===================================================================
@@ -134,6 +134,7 @@
     NO_shglob
     NO_ksharrays
     NO_cshnullglob
+    NO_allexport
 )
 
 # These can hold names of functions that are to be called before/after all
Index: Src/params.c
===================================================================
@@ -1484,7 +1484,19 @@
 {
     char buf[(sizeof(zlong) * 8) + 4], *val;
 
-    if (PM_TYPE(pm->flags) == PM_INTEGER)
+    if (PM_TYPE(pm->flags) & (PM_ARRAY|PM_HASHED)) {
+#if 0	/* Requires changes elsewhere in params.c and builtin.c */
+	if (emulation == EMULATE_KSH /* isset(KSHARRAYS) */) {
+	    struct value v;
+	    v.isarr = 1;
+	    v.inv = 0;
+	    v.start = 0;
+	    v.end = -1;
+	    val = getstrvalue(&v);
+	} else
+#endif
+	    return;
+    } else if (PM_TYPE(pm->flags) == PM_INTEGER)
 	convbase(val = buf, pm->gets.ifn(pm), pm->ct);
     else if (pm->flags & (PM_EFLOAT|PM_FFLOAT))
 	val = convfloat(pm->gets.ffn(pm), pm->ct,

-- 
Bart Schaefer                                 Brass Lantern Enterprises
http://www.well.com/user/barts              http://www.brasslantern.com

Zsh: http://www.zsh.org | PHPerl Project: http://phperl.sourceforge.net   

